<header id="page-topbar">
    <div class="navbar-header">
        <div class="d-flex">
            <!-- LOGO -->
            <div class="navbar-brand-box">
                <a href="{{ path('dashboard') }}" class="logo logo-dark">
                    <span class="logo-sm">
                        <img src="{{ asset('images/p.png') }}" alt="" height="55">
                    </span>
                    <span class="logo-lg">
                        <img src="{{ asset('images/prodeta.png') }}" alt="" height="55">
                    </span>
                </a>

                {# <a href="{{ path('dashboard') }}" class="logo logo-light">
                    <span class="logo-sm">
                        <img src="assets/images/logo-sm.png" alt="" height="22">
                    </span>
                    <span class="logo-lg">
                        <img src="assets/images/logo-light.png" alt="" height="22">
                    </span>
                </a> #}
            </div>

            <button type="button" class="btn btn-sm px-3 font-size-16 header-item vertical-menu-btn">
                <i class="fa fa-fw fa-bars"></i>
            </button>

        </div>

        <div class="d-flex">
            <div class="dropdown d-inline-block">
                <button type="button" class="btn header-item user text-start d-flex align-items-center" id="page-header-user-dropdown"
                    data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <img class="rounded-circle header-profile-user" src="{{ asset('images/user_img.png') }}"
                    alt="Header Avatar">
                    <span class="ms-2 d-none d-sm-block user-item-desc">
                        <span class="user-name">{{ app.user.completeName }}</span>
                        <span class="user-sub-title">{{ app.user.poste }}</span>
                    </span>
                </button>
                <div class="dropdown-menu dropdown-menu-end pt-0">
                    <div class="p-3 bg-success border-bottom">
                        <h6 class="mb-0 text-white">{{ app.user.email }}</h6>
                        {# <p class="mb-0 font-size-11 text-white-50 fw-semibold">{{ app.user.email }}</p> #}
                    </div>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">
                        <i class="mdi mdi-logout text-muted font-size-16 align-middle me-1"></i>
                        <span class="align-middle">Déconnexion</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</header>